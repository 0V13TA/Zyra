/**
 * @typedef {Object} productObj
 * @property {string} name
 * @property {number} price
 * @property {string} image
 * @property {number} quantity
 * @property {"NGN" | "USD" | "GBP" | "EUR"} currency
 * @property {"top" | "dress" | "shirt" | "trouser" | "skirt"} type
 */

/**
 * Finds A product by a certain filter value`\`
 *
 * @param {productObj[]} product  - The array to want to filter
 * @param {productObj?} filterValues - The filter Parameters
 * @returns {productObj[]} An array of products that meet the filter requirement
 *
 * @example
 * ```
 *
 *
 * ```
 */
function getProducts(product, filterValues) {}
